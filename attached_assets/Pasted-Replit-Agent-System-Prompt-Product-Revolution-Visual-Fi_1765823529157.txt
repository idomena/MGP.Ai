Replit Agent – System Prompt (Product Revolution / Visual-First App)

Role:
You are rebuilding this application to a publish-ready, premium fitness product.
The current state is not acceptable for release.

⸻

Critical Product Rule

The app must be visual-first.
	•	Users must never be sent to external links to “find” content.
	•	Users must never be required to search manually for exercise videos.
	•	The app must show content directly inside the UI.

⸻

Exercise & Workout Assistance (Non-Negotiable)

When a user:
	•	Asks a question about an exercise
	•	Opens a workout
	•	Taps an unfamiliar movement

The app must immediately display:
	•	An embedded video OR animated GIF
	•	Clear visual demonstration
	•	Minimal text (only supportive, not primary)

❌ No links
❌ No redirects
❌ No “learn more” buttons

✅ Instant visual feedback

⸻

UI / UX Direction
	•	The app must feel:
	•	Modern
	•	Clean
	•	Premium
	•	Confident
	•	Remove clutter, remove explanation-heavy screens.
	•	Replace text blocks with:
	•	Cards
	•	Media previews
	•	Motion (GIFs / short loops)

⸻

Homepage & Flow
	•	Homepage must feel alive:
	•	Map reflects today (server time)
	•	Current workout is visually highlighted
	•	The user should always know:
	•	What to do now
	•	What’s coming next
	•	Without reading instructions

⸻

Media Architecture
	•	Maintain a structured media system:
	•	Each exercise → video/GIF asset
	•	Fast loading
	•	Cached when possible
	•	Media is part of the core experience, not an add-on.

⸻

Quality Bar

If the app:
	•	Feels like a tool instead of a product
	•	Explains instead of shows
	•	Sends users elsewhere for understanding

→ The implementation is not acceptable.

⸻

Final Requirement

This app must feel like a next-generation fitness experience, not a checklist app.

Rebuild behavior, flow, and presentation accordingly.

⸻

Important (real talk, product-level)

You’re at the exact point founders hit when:
	•	The idea is right
	•	The execution is not there yet

That’s normal.
Stopping and rebuilding now is the right instinct — not a failure.

⸻

Next step (I recommend this order)

If you want, I’ll next:
	1.	Design the exercise → video/GIF system
	2.	Propose a homepage redesign logic